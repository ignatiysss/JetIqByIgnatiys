<resources>

    <!-- Base application theme. -->
    <style name="Base.Theme.JetIq" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="customBackgroundColor">@color/colorPrimary</item>
        <item name="customCircleColor">@color/colorForCircleBackgroundForItemSubject</item>
        <item name="customTextColorForToolBar">@color/colorText</item>
        <item name="customItemColorForToolBar">@drawable/baseline_settings_white_24</item>
        <item name="android:itemTextAppearance">@color/colorText</item>

    </style>

    <style name="Theme.JetIq" parent="Base.Theme.JetIq" />



    <style name="Theme.MyApplicationNavDrawer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MyApplicationNavDrawer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
